import 'package:flutter_currency_converter/app/models/currency_model.dart';

class ArgSelect {
  final String where;
  final List<CurrencyModel> item;
  ArgSelect({required this.item,required this.where});
}